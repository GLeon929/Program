#what is a Boolean?
#Boolean is a true or false data type
#examples
score=85
print(score>80)
print(score<90)
print(score==85)

#comparison operators
# == (equal to)
# > (greater than)
# < (less than)
# >= (greater than or equal to)
# <= (less than or equal to)
# != (not equal to)

#how do we compare 3 values?
#we can use the and operator to compare multiple values
temp=72
if temp >=68 and temp <=75:
    print("The temperature is comfortable.")
#we use "and"  both conditions must be true for the overall condition to be true
#when using or at least one condition must be true 
day="Saturday"
is_holiday=False
if day=="Saturday" or is_holiday==True:
    print("You can sleep in.")
#chain comparison operators is when we compare multiple values in a single statement
#example
score=75
if 70<=score<=80:
    print("You passed the test.")
#give me a conditional program that prints out ship[ping cost depending on total price
total=float(input("Enter your total order: "))
if total<25:
    print("Shipping is $10")
elif total<=50:
    print("Shipping is $5")
else:
    print("Shipping is free")


list1=[1,2,3]
list2=[1,2,3]
list3=list1
print(list1==list2) #True because the values are the same
print(list1 is list2)
print(list1==list3) #True because the values are the same
print(list1 is list3) #True because they are the same object in memory